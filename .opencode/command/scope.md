---
description: Scoping stage - converts the approved Discovery artifact into scoped, testable requirements in docs/requirements.md. Requires docs/discovery.md approved.
agent: scopper
---

Run one Scoping cycle.

Additional context from the human (optional): $ARGUMENTS

Follow your complete role definition. Validate that `docs/discovery.md` is ✅ Approved before working; if it is not, stop and say so. Produce `docs/requirements.md` from your output template exactly, then stop and hand it to the human for approval.
