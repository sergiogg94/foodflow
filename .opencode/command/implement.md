---
description: Implementation stage - implements approved tasks exactly as specified by the ADR on a feature branch, with commits referencing task IDs and an implementation notes document. Requires requirements, plan, and ADR(s) approved.
agent: developer
---

Run one Implementation cycle.

Task(s) to implement: $ARGUMENTS

Follow your complete role definition. Validate that the requirements, the implementation plan, and the covering ADR(s) are ✅ Approved before writing any code; if any input is missing or unapproved, stop and say so. Implement only the assigned task(s) exactly as specified, commit on a feature branch, emit the implementation notes document, then stop - validation and review run as separate commands.
